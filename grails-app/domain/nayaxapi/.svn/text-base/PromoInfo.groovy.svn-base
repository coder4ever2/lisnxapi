package nayaxapi

import java.util.Date;

class PromoInfo {
	
	String fid
	String imagePath
	Date dateCreated
	Date lastUpdated

    static constraints = {
		fid(nullable:false)
		imagePath(nullable:false)
		dateCreated(nullable: true)
		lastUpdated(nullable:true)
    }
}
